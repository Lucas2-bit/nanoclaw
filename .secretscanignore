# scripts/scan-secrets.cjs ignore list.
# One path per line. Trailing-slash entries match a directory and its contents.
# Lines starting with '#' are comments. Examples:
#   docs/sample-token.md
#   fixtures/
#
# Note: node_modules/, dist/, and .git/ are hard-excluded by the scanner
# itself — you do not need to list them here.

